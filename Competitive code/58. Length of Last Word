class Solution {
    public int lengthOfLastWord(String s) {
       String arr[]=s.split(" ");
      int i=arr.length-1;
        if(i<0)
        {
            return 0;
        }
        char arr2[]=arr[i].toCharArray();
             return arr2.length;
        
        
    }
}
